import { NotFoundComponent, ScreenTemplate } from '@/components';

const NotFound = () => {
  return (
    <ScreenTemplate>
      <NotFoundComponent />
    </ScreenTemplate>
  );
};

export default NotFound;
